
- The jar file is run by double-clicking run.bat

- The XML file to be used should be under the namespace "database.xml"
  and placed in the same folder as the .bat and .jar files

- The project is distributed into 4 packages:
gui, tablesanddishes, users, xml

- In the UML Diagram: constructors, setters and getters are ignored